name: CI

on:
  push:
    branches: [dev, beta]
  pull_request:
    branches: [dev, beta]

jobs:
  # Every job carries a timeout. Without one GitHub applies its 6-hour default,
  # so a job that wedges holds a runner and blocks the PR status for the rest of
  # the day. Observed on run 31501095075: the `test` job reported every step —
  # including "Complete job" — as successful, yet the job never finalised and the
  # run sat in_progress for 30+ minutes. The cancel API returned HTTP 500, so it
  # could not even be stopped by hand. These limits are sized several times the
  # observed runtime so they only trip on a genuine stall, never on a slow run.
  lint:
    runs-on: ubuntu-latest
    # Observed ~1 min.
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint

  test:
    runs-on: ubuntu-latest
    # Observed ~2-3 min for 70 suites / 975 tests with coverage.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run test:ci

  # Build the production bundle once and share it with all e2e shards.
  # This removes the per-shard build cost (~60-90s) from the e2e critical path.
  build:
    runs-on: ubuntu-latest
    # Observed ~1 min.
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          REACT_APP_REGISTRATION_API: ${{ vars.REACT_APP_REGISTRATION_API }}
          REACT_APP_PANTRY_FINDER_API: ${{ vars.REACT_APP_PANTRY_FINDER_API }}
          REACT_APP_AWS_REGION: ${{ vars.REACT_APP_AWS_REGION }}
          REACT_APP_USER_POOL_ID: ${{ vars.REACT_APP_USER_POOL_ID }}
          REACT_APP_USER_POOL_CLIENT_ID: ${{ vars.REACT_APP_USER_POOL_CLIENT_ID }}
          REACT_APP_CLIENT_URL: ${{ vars.REACT_APP_CLIENT_URL }}
          REACT_APP_FRESHTRAK_PARTNERS_URL: ${{ vars.REACT_APP_FRESHTRAK_PARTNERS_URL }}
          REACT_APP_GTM_ID: ${{ vars.REACT_APP_GTM_ID }}
          REACT_APP_IMAGES_BASE_URL: ${{ vars.REACT_APP_IMAGES_BASE_URL }}
          REACT_APP_GOOGLE_API_KEY: ${{ secrets.REACT_APP_GOOGLE_API_KEY }}
          REACT_APP_GOOGLE_GEOLOCATION_KEY: ${{ secrets.REACT_APP_GOOGLE_GEOLOCATION_KEY }}
      # index.html loads /runtime-config.js to populate window._env_, which is the
      # only config source that works in the built bundle. The vite `define` block
      # cannot substitute for it: define does literal text replacement of
      # `process.env.REACT_APP_X`, but src/config.ts reads keys dynamically
      # (viteEnv[reactAppKey]), so no replacement ever occurs and `process` is
      # undefined in the browser. Without this file every config value is "" and
      # Cognito sign-in fails, taking the whole e2e suite with it.
      #
      # Generated here, before the artifact upload, so e2e-setup and all shards
      # inherit it via app-build rather than each regenerating it.
      - name: Generate runtime config
        run: |
          echo "window._env_ = {" > dist/runtime-config.js
          env | grep '^REACT_APP_' | while IFS='=' read -r name value; do
            key="${name#REACT_APP_}"
            echo "  $key: \"$value\"," >> dist/runtime-config.js
          done
          echo "};" >> dist/runtime-config.js
          echo "--- generated keys ---"
          grep -o '^  [A-Z_]*:' dist/runtime-config.js || echo "WARNING: no REACT_APP_* vars found"
        env:
          REACT_APP_REGISTRATION_API: ${{ vars.REACT_APP_REGISTRATION_API }}
          REACT_APP_PANTRY_FINDER_API: ${{ vars.REACT_APP_PANTRY_FINDER_API }}
          REACT_APP_AWS_REGION: ${{ vars.REACT_APP_AWS_REGION }}
          REACT_APP_USER_POOL_ID: ${{ vars.REACT_APP_USER_POOL_ID }}
          REACT_APP_USER_POOL_CLIENT_ID: ${{ vars.REACT_APP_USER_POOL_CLIENT_ID }}
          REACT_APP_CLIENT_URL: ${{ vars.REACT_APP_CLIENT_URL }}
          REACT_APP_FRESHTRAK_PARTNERS_URL: ${{ vars.REACT_APP_FRESHTRAK_PARTNERS_URL }}
          REACT_APP_GTM_ID: ${{ vars.REACT_APP_GTM_ID }}
          REACT_APP_IMAGES_BASE_URL: ${{ vars.REACT_APP_IMAGES_BASE_URL }}
          REACT_APP_GOOGLE_API_KEY: ${{ secrets.REACT_APP_GOOGLE_API_KEY }}
          REACT_APP_GOOGLE_GEOLOCATION_KEY: ${{ secrets.REACT_APP_GOOGLE_GEOLOCATION_KEY }}
      - uses: actions/upload-artifact@v6
        with:
          name: app-build
          # Vite outputs to dist/ by default (no custom outDir in vite.config.mts)
          path: dist/
          retention-days: 1

  # Run auth.setup.ts exactly once to produce the Cognito session state.
  # Uploading it as an artifact lets all three shards reuse it without
  # triggering additional sign-ins, avoiding Cognito rate-limit collisions.
  # This job also warms the Playwright browser cache so shards start faster.
  e2e-setup:
    runs-on: ubuntu-latest
    needs: build
    # Worst-case on a cold browser cache: npm ci (~2 min) + playwright install
    # for three browsers (~4 min) + up to three 180s Cognito login attempts
    # (~9 min) = ~15 min. 20 minutes gives a comfortable buffer.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v6
        with:
          name: app-build
          path: dist/

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: pw-cache
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install Playwright browsers (cache miss)
        if: steps.pw-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Install Playwright OS dependencies (cache hit)
        if: steps.pw-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium firefox webkit

      # Pre-create the output directory so Playwright's storageState() has a
      # parent directory to write into on all runner environments.
      - name: Create auth state directory
        run: mkdir -p e2e/.auth

      - name: Authenticate once
        run: npx playwright test --project=setup
        env:
          PLAYWRIGHT_BASE_URL: http://localhost:5000
          E2E_TEST_EMAIL: ${{ secrets.E2E_TEST_EMAIL }}
          E2E_TEST_PASSWORD: ${{ secrets.E2E_TEST_PASSWORD }}
          REACT_APP_REGISTRATION_API: ${{ vars.REACT_APP_REGISTRATION_API }}
          REACT_APP_PANTRY_FINDER_API: ${{ vars.REACT_APP_PANTRY_FINDER_API }}
          REACT_APP_AWS_REGION: ${{ vars.REACT_APP_AWS_REGION }}
          REACT_APP_USER_POOL_ID: ${{ vars.REACT_APP_USER_POOL_ID }}
          REACT_APP_USER_POOL_CLIENT_ID: ${{ vars.REACT_APP_USER_POOL_CLIENT_ID }}
          REACT_APP_CLIENT_URL: ${{ vars.REACT_APP_CLIENT_URL }}
          REACT_APP_FRESHTRAK_PARTNERS_URL: ${{ vars.REACT_APP_FRESHTRAK_PARTNERS_URL }}
          REACT_APP_GTM_ID: ${{ vars.REACT_APP_GTM_ID }}
          REACT_APP_IMAGES_BASE_URL: ${{ vars.REACT_APP_IMAGES_BASE_URL }}
          REACT_APP_GOOGLE_API_KEY: ${{ secrets.REACT_APP_GOOGLE_API_KEY }}
          REACT_APP_GOOGLE_GEOLOCATION_KEY: ${{ secrets.REACT_APP_GOOGLE_GEOLOCATION_KEY }}

      - name: Upload auth state
        uses: actions/upload-artifact@v6
        with:
          name: e2e-auth-state
          # Upload by explicit file path rather than directory to avoid hidden-
          # directory glob quirks. Fails the job immediately if the file wasn't
          # produced, so downstream shards get a clear dependency failure rather
          # than an opaque "artifact not found" error.
          path: e2e/.auth/user.json
          retention-days: 1
          if-no-files-found: error

  # e2e runs as a 3-way shard matrix so each runner handles ~1/3 of the tests
  # across all three browsers in parallel, cutting wall-clock time by ~60%.
  # Shards depend on e2e-setup so the Cognito auth state and browser cache are
  # ready before any shard starts — no redundant sign-ins, no cache misses.
  e2e:
    runs-on: ubuntu-latest
    needs: [lint, test, e2e-setup]
    # Sized for one worker per shard (see the workers comment in
    # playwright.config.ts). A shard runs ~1/3 of the suite serially, so expect
    # ~16-20 min rather than the ~7 min seen at 4 workers. Kept well clear of
    # that so a slow-but-working run fails on a real assertion instead of an
    # ambiguous job timeout.
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        shard: ['1/3', '2/3', '3/3']
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v6
        with:
          name: app-build
          # Restore into dist/ to match Vite's output path expected by vite preview
          path: dist/

      - name: Download auth state
        uses: actions/download-artifact@v6
        with:
          name: e2e-auth-state
          # Restores user.json to e2e/.auth/ — auth.setup.ts detects the file
          # and skips re-authentication, preventing concurrent Cognito sign-ins.
          path: e2e/.auth/

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: pw-cache
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install Playwright browsers (cache miss)
        if: steps.pw-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Install Playwright OS dependencies (cache hit)
        if: steps.pw-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium firefox webkit

      - name: Run Playwright tests (shard ${{ matrix.shard }})
        run: npx playwright test --shard=${{ matrix.shard }}
        env:
          PLAYWRIGHT_BASE_URL: http://localhost:5000
          E2E_TEST_EMAIL: ${{ secrets.E2E_TEST_EMAIL }}
          E2E_TEST_PASSWORD: ${{ secrets.E2E_TEST_PASSWORD }}
          REACT_APP_REGISTRATION_API: ${{ vars.REACT_APP_REGISTRATION_API }}
          REACT_APP_PANTRY_FINDER_API: ${{ vars.REACT_APP_PANTRY_FINDER_API }}
          REACT_APP_AWS_REGION: ${{ vars.REACT_APP_AWS_REGION }}
          REACT_APP_USER_POOL_ID: ${{ vars.REACT_APP_USER_POOL_ID }}
          REACT_APP_USER_POOL_CLIENT_ID: ${{ vars.REACT_APP_USER_POOL_CLIENT_ID }}
          REACT_APP_CLIENT_URL: ${{ vars.REACT_APP_CLIENT_URL }}
          REACT_APP_FRESHTRAK_PARTNERS_URL: ${{ vars.REACT_APP_FRESHTRAK_PARTNERS_URL }}
          REACT_APP_GTM_ID: ${{ vars.REACT_APP_GTM_ID }}
          REACT_APP_IMAGES_BASE_URL: ${{ vars.REACT_APP_IMAGES_BASE_URL }}
          REACT_APP_GOOGLE_API_KEY: ${{ secrets.REACT_APP_GOOGLE_API_KEY }}
          REACT_APP_GOOGLE_GEOLOCATION_KEY: ${{ secrets.REACT_APP_GOOGLE_GEOLOCATION_KEY }}

      - name: Upload blob report
        uses: actions/upload-artifact@v6
        # always() ensures upload runs whether tests passed, failed, or the
        # shard was cancelled mid-run — partial results are still mergeable.
        # if-no-files-found: ignore handles the edge case where Playwright
        # exited before writing any output (e.g. webServer failed to start).
        if: always()
        with:
          name: blob-report-${{ strategy.job-index }}
          path: blob-report/
          retention-days: 1
          if-no-files-found: ignore

  # Merge the per-shard blob reports into a single HTML report once all
  # shards finish (whether they passed or failed).
  e2e-report:
    runs-on: ubuntu-latest
    needs: e2e
    # Observed <1 min; downloading and merging blob reports is I/O-bound.
    timeout-minutes: 15
    # Run when shards finished (success or failure) — both states mean blob
    # reports were uploaded and can be merged. Explicitly exclude 'skipped'
    # (e.g. e2e-setup failed so e2e never ran) and 'cancelled' (no artifacts
    # uploaded), which a bare always() would incorrectly include.
    #
    # The !cancelled() call is required, not stylistic: a condition built only
    # from needs.*.result comparisons does not override the implicit skip that
    # GitHub applies when a needed job fails, so this job was silently skipped
    # on exactly the runs whose report was worth reading. Including an
    # always()-family function opts out of that skip, and the result check then
    # narrows it back down to the two states that produced artifacts.
    if: ${{ !cancelled() && (needs.e2e.result == 'success' || needs.e2e.result == 'failure') }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci

      - name: Download all blob reports
        uses: actions/download-artifact@v6
        with:
          pattern: blob-report-*
          merge-multiple: true
          path: all-blob-reports/

      - name: Merge reports
        run: npx playwright merge-reports --reporter html ./all-blob-reports

      - uses: actions/upload-artifact@v6
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14
